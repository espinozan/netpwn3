click==8.1.3         # Para crear la interfaz de línea de comandos (CLI)
pandas==2.1.1        # Para manejar datos (e.g., lista de dispositivos escaneados)
scapy==2.5.0         # Para manipulación y captura de paquetes de red
cryptography==41.0.3 # Para cifrado y descifrado de mensajes con Fernet
